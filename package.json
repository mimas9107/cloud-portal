{
  "name": "cloud-services-monitor",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "publish:dashboard": "wrangler pages deploy ./dashboard --project-name=cloud-services-dashboard"
  },
  "devDependencies": {
    "wrangler": "^4.86.0"
  }
}
